language: python
cache: pip
python:
  - "3.6"
install:
  - "pip install -r requirements.txt"
script:
  - py.test tests/unit --cov=tohsaka --cov-report html --cov-report term
after_success:
  - coveralls